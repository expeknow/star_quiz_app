<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StarQuizApp"
        tools:targetApi="31">
        <activity
            android:name="com.expeknow.starquizapp.QuizBooleanQuestionActivity"
            android:exported="false" />
        <activity
            android:name="com.expeknow.starquizapp.ResultActivity"
            android:exported="false" />
        <activity
            android:name="com.expeknow.starquizapp.QuizMultipleQuestionActivity"
            android:exported="false"
            android:label="@string/title_activity_quiz_question"
            android:theme="@style/Theme.StarQuizApp.NoActionBar" />
        <activity
            android:name="com.expeknow.starquizapp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>